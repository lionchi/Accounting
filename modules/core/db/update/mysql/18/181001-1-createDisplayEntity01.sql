create table ACCOUNTING_DISPLAY_ENTITY (
    ID bigint,
    UUID varchar(32),
    --
    MANUF_ID varchar(100),
    NAME varchar(100),
    DIAGONAL varchar(50),
    PK_ID bigint,
    --
    primary key (ID)
);
